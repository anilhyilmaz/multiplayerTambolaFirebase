import 'package:flutter/material.dart';

class ControlPickedNumbersScreen extends StatelessWidget {
  const ControlPickedNumbersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
